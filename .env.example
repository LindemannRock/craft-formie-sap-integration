# SAP Integration for Formie - Environment Variables
# Copy this file to your project's .env file and configure the values

# ===========================================
# HOW TO USE THESE VARIABLES
# ===========================================
# In the Craft CP, when configuring the SAP integration,
# use the format: $VARIABLE_NAME
# For example: $FORMIE_SAP_CLIENT_ID
# This tells Craft to read the value from the environment

# ===========================================
# SAP API CREDENTIALS (Required)
# ===========================================

# OAuth Client ID for SAP API authentication
# In Craft CP, enter: $FORMIE_SAP_CLIENT_ID
FORMIE_SAP_CLIENT_ID="your-sap-client-id"

# OAuth Client Secret for SAP API authentication
# In Craft CP, enter: $FORMIE_SAP_CLIENT_SECRET
FORMIE_SAP_CLIENT_SECRET="your-sap-client-secret"

# ===========================================
# SAP ENVIRONMENT SETTINGS
# ===========================================

# Environment mode: 'staging' or 'production'
# In Craft CP, enter: $FORMIE_SAP_ENVIRONMENT
FORMIE_SAP_ENVIRONMENT="staging"

# Staging API Base URL (without trailing slash)
# Example: https://staging-api.sap.com/v1
# In Craft CP, enter: $FORMIE_SAP_STAGING_URL
FORMIE_SAP_STAGING_URL="https://staging-api.sap.com/v1"

# Production API Base URL (without trailing slash)
# Example: https://production-api.sap.com/v1
# In Craft CP, enter: $FORMIE_SAP_PRODUCTION_URL
FORMIE_SAP_PRODUCTION_URL="https://production-api.sap.com/v1"

# ===========================================
# SAP API ENDPOINTS
# ===========================================

# Custom API endpoint path (will be appended to base URL)
# Default: /customer-feedback
# In Craft CP, enter: $FORMIE_SAP_API_ENDPOINT
FORMIE_SAP_API_ENDPOINT="/customer-feedback"

# ===========================================
# OAUTH SETTINGS (Future Use)
# ===========================================
# Note: OAuth is not currently implemented in this integration.
# These settings are placeholders for future OAuth support.

# OAuth Authorization URL (if using custom OAuth server)
# In Craft CP, enter: $FORMIE_SAP_OAUTH_AUTHORIZE_URL
FORMIE_SAP_OAUTH_AUTHORIZE_URL=""

# OAuth Token URL (if using custom OAuth server)
# In Craft CP, enter: $FORMIE_SAP_OAUTH_TOKEN_URL
FORMIE_SAP_OAUTH_TOKEN_URL=""

# OAuth Redirect URI (if using custom redirect)
# Leave empty to use Craft's default integration redirect
# Example: https://yoursite.com/actions/formie/integrations/callback
# In Craft CP, enter: $FORMIE_SAP_REDIRECT_URI
FORMIE_SAP_REDIRECT_URI=""

# OAuth Scope (comma-separated list of scopes)
# Default: customer_data:write,forms:submit
# In Craft CP, enter: $FORMIE_SAP_OAUTH_SCOPE
FORMIE_SAP_OAUTH_SCOPE="customer_data:write,forms:submit"

# ===========================================
# WEBHOOK TESTING
# ===========================================

# For testing with webhook.site or similar services
# Uncomment and set these values to override production URLs

# WEBHOOK_TEST_URL="https://webhook.site/your-unique-url"
# FORMIE_SAP_STAGING_URL="${WEBHOOK_TEST_URL}"
# FORMIE_SAP_CLIENT_ID="test-client-id"
# FORMIE_SAP_CLIENT_SECRET="test-client-secret"